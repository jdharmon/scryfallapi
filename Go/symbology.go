package

// Code generated by Microsoft (R) AutoRest Code Generator.
// Changes may cause incorrect behavior and will be lost if the code is regenerated.

import (
    "github.com/Azure/go-autorest/autorest"
    "github.com/Azure/go-autorest/autorest/azure"
    "net/http"
    "context"
)

// SymbologyClient is the client for the Symbology methods of the  service.
type SymbologyClient struct {
    BaseClient
}
// NewSymbologyClient creates an instance of the SymbologyClient client.
func NewSymbologyClient() SymbologyClient {
    return NewSymbologyClientWithBaseURI(DefaultBaseURI, )
}

// NewSymbologyClientWithBaseURI creates an instance of the SymbologyClient client.
    func NewSymbologyClientWithBaseURI(baseURI string, ) SymbologyClient {
        return SymbologyClient{ NewWithBaseURI(baseURI, )}
    }

// GetAllMethod sends the get all request.
func (client SymbologyClient) GetAllMethod(ctx context.Context) (result CardSymbolListType, err error) {
    req, err := client.GetAllMethodPreparer(ctx)
    if err != nil {
    err = autorest.NewErrorWithError(err, ".SymbologyClient", "GetAllMethod", nil , "Failure preparing request")
    return
    }

        resp, err := client.GetAllMethodSender(req)
        if err != nil {
        result.Response = autorest.Response{Response: resp}
        err = autorest.NewErrorWithError(err, ".SymbologyClient", "GetAllMethod", resp, "Failure sending request")
        return
        }

        result, err = client.GetAllMethodResponder(resp)
        if err != nil {
        err = autorest.NewErrorWithError(err, ".SymbologyClient", "GetAllMethod", resp, "Failure responding to request")
        }

    return
    }

    // GetAllMethodPreparer prepares the GetAllMethod request.
    func (client SymbologyClient) GetAllMethodPreparer(ctx context.Context) (*http.Request, error) {
    preparer := autorest.CreatePreparer(
    autorest.AsGet(),
    autorest.WithBaseURL(client.BaseURI),
    autorest.WithPath("/symbology"))
    return preparer.Prepare((&http.Request{}).WithContext(ctx))
    }

    // GetAllMethodSender sends the GetAllMethod request. The method will close the
    // http.Response Body if it receives an error.
    func (client SymbologyClient) GetAllMethodSender(req *http.Request) (*http.Response, error) {
        return autorest.SendWithSender(client, req,
        autorest.DoRetryForStatusCodes(client.RetryAttempts, client.RetryDuration, autorest.StatusCodesForRetry...))
            }

    // GetAllMethodResponder handles the response to the GetAllMethod request. The method always
    // closes the http.Response Body.
    func (client SymbologyClient) GetAllMethodResponder(resp *http.Response) (result CardSymbolListType, err error) {
        err = autorest.Respond(
        resp,
        client.ByInspecting(),
        azure.WithErrorUnlessStatusCode(http.StatusOK),
        autorest.ByUnmarshallingJSON(&result),
        autorest.ByClosing())
        result.Response = autorest.Response{Response: resp}
            return
    }

// ParseManaMethod sends the parse mana request.
    //
func (client SymbologyClient) ParseManaMethod(ctx context.Context, cost string) (result ManaCostType, err error) {
    req, err := client.ParseManaMethodPreparer(ctx, cost)
    if err != nil {
    err = autorest.NewErrorWithError(err, ".SymbologyClient", "ParseManaMethod", nil , "Failure preparing request")
    return
    }

        resp, err := client.ParseManaMethodSender(req)
        if err != nil {
        result.Response = autorest.Response{Response: resp}
        err = autorest.NewErrorWithError(err, ".SymbologyClient", "ParseManaMethod", resp, "Failure sending request")
        return
        }

        result, err = client.ParseManaMethodResponder(resp)
        if err != nil {
        err = autorest.NewErrorWithError(err, ".SymbologyClient", "ParseManaMethod", resp, "Failure responding to request")
        }

    return
    }

    // ParseManaMethodPreparer prepares the ParseManaMethod request.
    func (client SymbologyClient) ParseManaMethodPreparer(ctx context.Context, cost string) (*http.Request, error) {
                queryParameters := map[string]interface{} {
        "cost": autorest.Encode("query",cost),
        }

    preparer := autorest.CreatePreparer(
    autorest.AsGet(),
    autorest.WithBaseURL(client.BaseURI),
    autorest.WithPath("/symbology/parse-mana"),
    autorest.WithQueryParameters(queryParameters))
    return preparer.Prepare((&http.Request{}).WithContext(ctx))
    }

    // ParseManaMethodSender sends the ParseManaMethod request. The method will close the
    // http.Response Body if it receives an error.
    func (client SymbologyClient) ParseManaMethodSender(req *http.Request) (*http.Response, error) {
        return autorest.SendWithSender(client, req,
        autorest.DoRetryForStatusCodes(client.RetryAttempts, client.RetryDuration, autorest.StatusCodesForRetry...))
            }

    // ParseManaMethodResponder handles the response to the ParseManaMethod request. The method always
    // closes the http.Response Body.
    func (client SymbologyClient) ParseManaMethodResponder(resp *http.Response) (result ManaCostType, err error) {
        err = autorest.Respond(
        resp,
        client.ByInspecting(),
        azure.WithErrorUnlessStatusCode(http.StatusOK,http.StatusCreated),
        autorest.ByUnmarshallingJSON(&result),
        autorest.ByClosing())
        result.Response = autorest.Response{Response: resp}
            return
    }

